<h1>All Entries</h1>

<ul id="entries">
  <% @entries.each do |entry| %>
    <li>
      <%= link_to entry.title, entry_path(entry), class: 'entry_title' %>
    </li>
  <% end %>
</ul>

<%= link_to "Create a new entry", new_entry_path, class: "new_entry" %>